import { TestBed } from '@angular/core/testing';

import { CrawlstartService } from './crawlstart.service';

describe('CrawlstartService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [CrawlstartService]
    });
  });
});

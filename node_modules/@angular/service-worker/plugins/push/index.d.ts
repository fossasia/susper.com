import { Operation, Plugin, PluginFactory, VersionWorker } from '@angular/service-worker/worker';
export declare function Push(): PluginFactory<PushImpl>;
export declare class PushImpl implements Plugin<PushImpl> {
    private worker;
    private streams;
    private buffer;
    private readonly pushManifest;
    constructor(worker: VersionWorker);
    setup(ops: Operation[]): void;
    message(message: any, id: number): void;
    messageClosed(id: number): void;
    push(data: any): void;
    maybeShowNotification(data: any): void;
}

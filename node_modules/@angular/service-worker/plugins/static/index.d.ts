import { FetchInstruction, Operation, Plugin, PluginFactory, VersionWorker } from '@angular/service-worker/worker';
export interface StaticContentCacheOptions {
    manifestKey?: string;
}
export declare function StaticContentCache(options?: StaticContentCacheOptions): PluginFactory<StaticContentCacheImpl>;
export declare class StaticContentCacheImpl implements Plugin<StaticContentCacheImpl> {
    worker: VersionWorker;
    key: string;
    private cacheKey;
    constructor(worker: VersionWorker, key: string);
    private readonly staticManifest;
    private shouldCacheBustFn();
    setup(operations: Operation[]): void;
    update(operations: Operation[], previous: StaticContentCacheImpl): void;
    fetch(req: Request): FetchInstruction;
    cleanup(operations: Operation[]): void;
    validate(): Promise<boolean>;
}

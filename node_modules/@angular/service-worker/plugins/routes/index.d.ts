import { FetchInstruction, Operation, Plugin, PluginFactory, VersionWorker } from '@angular/service-worker/worker';
export declare function RouteRedirection(): PluginFactory<RouteRedirectionImpl>;
export declare class RouteRedirectionImpl implements Plugin<RouteRedirectionImpl> {
    worker: VersionWorker;
    constructor(worker: VersionWorker);
    private readonly routeManifest;
    private hasExtension(path);
    setup(operations: Operation[]): void;
    fetch(req: Request): FetchInstruction;
}

import { FetchInstruction, Operation, Plugin, PluginFactory, VersionWorkerImpl } from '@angular/service-worker/worker';
export interface ExternalContentCacheOptions {
    manifestKey?: string;
}
export declare function ExternalContentCache(options?: ExternalContentCacheOptions): PluginFactory<ExternalPlugin>;
export declare class ExternalPlugin implements Plugin<ExternalPlugin> {
    worker: VersionWorkerImpl;
    key: string;
    private cacheKey;
    constructor(worker: VersionWorkerImpl, key: string);
    private readonly externalManifest;
    setup(operations: Operation[]): void;
    fetch(req: Request): FetchInstruction;
}

export declare type UrlMatchType = "exact" | "prefix" | "regex";
export interface UrlConfig {
    match?: UrlMatchType;
}
export declare class UrlMatcher {
    pattern: string;
    scope: string;
    match: UrlMatchType;
    private _regex;
    constructor(pattern: string, config: UrlConfig, scope: string);
    matches(url: string): boolean;
}
